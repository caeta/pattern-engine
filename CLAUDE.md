# pattern-engine — Claude への案内

## 目的

このvaultは、様々な領域のパターンランゲージ（建築・ソフトウェア・組織開発・創造実践など）を`raw/pattern-languages/`に蓄積し、それらを使って何らかのアウトプットを生み出す**パターンエンジン**を構築することを目的とする。

「パターンエンジン」という言葉は、明文化されたパターンランゲージ・パターンと、組織の活動から生まれる個人的・共有された知識、外部のパターンコミュニティとのあいだを媒介する装置を指す図（活動する→パターンを生成する／獲得する→パターンエンジンへ投入する→書く／改良する→裏打ちする→パターンランゲージへ、外部コミュニティとはインポート／エキスポートする、という循環）に由来する。このvaultはこの循環のうち、少なくとも「明文化されたパターンランゲージ・パターンの蓄積」（`raw/pattern-languages/`）を担う。

Andrej Karpathy の **LLM Wiki** 方式（AIが知識の追加・接続・整理を担う運用の型）は、[[LLMWIKI]]（`~/Documents/LLMWIKI`）と同じく、この目的を実現するための**実装手段**として採用する。ただし本vaultの`wiki/`層が具体的に何を生み出すか（パターンエンジンの図でいう「投入する」「書く／改良する」に相当する部分）は、まだ設計途中——`raw/pattern-languages/`にパターンランゲージが十分に蓄積されてから、`wiki/`の役割を継続検討する。

## 構造（現時点で決まっている部分）

```
raw/Clippings/   Web記事クリッピング（LLMWIKIと同じ位置づけ。変更しない）
raw/Files/       PDF・画像等の資料（変更しない）
raw/Comments/    wikiページへの人間からのコメント（LLMWIKIと同じ位置づけ）
raw/pattern-languages/    様々なパターンランゲージ本体。出典ごとにサブフォルダを切る
                 （例：raw/pattern-languages/アレクザンダー-パタンランゲージ/、
                       raw/pattern-languages/井庭崇-創造社会/）
                 個々のパターンはmdファイルの場合もあれば、
                 まとめてPDFで格納される場合もある。変更しない
wiki/            アウトプット層。何を置くかは継続検討（下記「未解決の問い」参照）
wiki/index.md    全ページのカタログ（type: 索引）
wiki/log.md      実行履歴（type: 履歴）。追記専用
wiki/daily/      日次更新まとめ（type: 日次更新）
schema/          運用ルール
Templates/       Obsidianのテンプレート置き場
```

`wiki/`配下のサブフォルダ構成（`concepts`・`patterns`（LLMWIKI流のガバナンス・パターンとは別概念）・`summaries`等に相当するもの）は未確定。`raw/pattern-languages/`の蓄積が進み、そこから何を「アウトプット」として引き出したいかが見えてきた段階で設計する。

## LLMWIKIとの関係

このvaultは[[LLMWIKI]]（`~/Documents/LLMWIKI`）とは**完全に別のリポジトリ**として運用する。LLMWIKIの目的（[[パーパスドリブンを支える共通言語コモンズ]]という富士通論文への接続）とこのvaultの目的（パターンランゲージそのものの蓄積とパターンエンジンの構築）は異なるため、CLAUDE.mdもgit履歴も分離する。LLM Wikiという実装方式だけを共有する関係にある。

LLMWIKIには既にパターンランゲージ関連の記事・人物ページ（[[井庭崇]]・[[クリストファー・アレクザンダー]]・[[Wikiの起源と進化]]等）が蓄積されているが、これらをこのvaultへ複製移動するか、LLMWIKI側に残してクロスリンクだけ張るかは未確定（下記「未解決の問い」参照）。

## 運用の基本方針（LLMWIKIから引き継ぐ部分）

- `raw/`のファイルは**変更しない**（元資料の汚染防止）
- 推測で事実を補わない
- リンクはファイル名で解決される。ファイル名の重複を避ける
- commitしたら、リモート（origin）が設定されている限り毎回すぐにpushする

## 未解決の問い

- `wiki/`層は何を生み出すべきか。パターンエンジンの図でいう「書く／改良する」（組織固有のパターンを明文化する）に相当するアウトプットを目指すのか、それとも複数のパターンランゲージを横断的に接続・比較する概念ページ（LLMWIKIの`wiki/patterns`に近いもの）を目指すのか
- `raw/pattern-languages/`に蓄積するパターンランゲージの出典サブフォルダの命名規則をどう統一するか
- LLMWIKIに既にあるパターンランゲージ関連資産（記事・人物ページ）を複製移動するか、参照のみに留めるか
- パターンランゲージの原文（特にアレクザンダー『パタン・ランゲージ』のような著作権保護された書籍）を`raw/pattern-languages/`にPDFで格納する場合、著作権上どこまで許容されるか。私的複製の範囲を超えないよう、vault自体を非公開にするか、PDFでなく要約・参照リンクに留めるかの判断が必要
